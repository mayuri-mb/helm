apiVersion: v1
kind: Service
metadata:
  name: nginx-headless
spec:
  type: {{ .values.service.type }}
  selector:
    project: roboshop
    component: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80 